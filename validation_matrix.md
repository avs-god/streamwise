# Streamwise Validation Matrix

This matrix records evidence for the current production-oriented implementation. **Synthetic browser** entries use intercepted in-memory responses only; they do not imply a real account, live catalog, or external public-web result was exercised.

For the strict one-row-per-workflow inventory with separate type, contract, browser, accessibility, responsive, and runtime columns, see `validation_index.md`.

## Explicit workflow ledger

| Delivery workflow | Contract and accessibility evidence | Browser and responsive evidence | Runtime evidence or declared limit |
|---|---|---|---|
| Country-aware legal search and offer categories | `catalog.test.ts`, provider hierarchy markup regressions, and `CatalogOfferPreview.test.tsx`. | Keyboard Discovery form plus desktop/mobile Discovery captures. | Catalog token remains absent; standby content is intentional. |
| Unified legal-catalog and public-web search compartments | `aiDiscovery.test.ts` and source-boundary markup tests. | Synthetic search verifies title correction, loading, grounded result, error, mobile source link, and no horizontal overflow. | Public-web material never becomes an offer. |
| Misspelling correction and source-link retention | `aiDiscovery.test.ts`. | Synthetic `Tencet` flow renders `Tenet` correction and inspectable link. | No live source result is claimed. |
| Provider-first title metadata and offers | `TitlePage.sourceBoundary.test.tsx` and provider-card tests. | Public title browser check and desktop/mobile no-token TitlePage capture. | Live metadata appears only after permitted catalog configuration. |
| Outbound-only IMDb, Rotten Tomatoes, and named critic reading | Title source-boundary, markup, and no-catalog regressions. | Public TitlePage browser check verifies unavailable state before resolution. | No score, review text, or rating data is imported. |
| Deterministic popular, top-rated, genre, similar, and adaptation disclosure | `catalog.test.ts`, `recommendations.test.ts`, and adaptation markup regression. | Keyboard route test and desktop/mobile Recommendations capture. | No-token state is candid; adaptations stay explicitly unsupported. |
| Private viewing signal recording | `Watchlist.viewingSignal.test.tsx` and data-access member-scope contract. | Synthetic member flow verifies the record action only follows explicit click, status error, and retry. | No inferred viewing history. |
| Private post-watch picks | `catalog.test.ts`, `recommendations.test.ts`, and viewing-signal contracts. | Synthetic flow verifies loading, failure, retry, populated card, and mobile containment. | No live catalog means no claimed real pick. |
| Watchlist snapshots and offer-change tracking | `availabilityTracking.test.ts` and tracking UI contracts. | Public Watchlist boundary test and prior responsive private-screen review. | Only confirmed catalog snapshots are compared. |
| Provider-specific alert preferences | `availabilityTracking.test.ts` and protected provider-subscription caller test. | Private UI is rendered by the Updates route; selected-provider logic has unit scope. | No provider account or public lead contributes to alerts. |
| Subscription wallet, decisions, renewals, and pause reviews | `dataAccess.test.ts`, alert-service tests, and tracking UI contracts. | Keyboard navigation covers Wallet, Decisions, and Updates. | Uses member-entered dates and costs only. |
| Managed scheduled refresh | `scheduledRefresh.test.ts` covers wrong, orphan, and no-token identity paths. | Not applicable. | Managed task `g4LWHU9zMwiNtvRLKVJN2x` returned HTTP 200 with `catalogConfigured:false`; see scheduler record. |
| Community posts, contribution sources, and leaving-soon leads | community input, visibility, privacy, and boundary suites. | Desktop/mobile Community capture verifies form and lead-disclosure layout. | Leads remain unverified and excluded from catalog/alerts. |
| Leaving Soon hub and public-web lead research | leaving-soon markup/data-access regressions and `aiDiscovery.test.ts`. | Desktop/mobile Leaving Soon review is documented in `recent_validation.md`. | No scraping, private-account monitoring, or lead-to-alert promotion. |
| Title-linked thread creation | community contract and markup regressions. | Synthetic title-context browser flow verifies auto-open context and thread selection. | No persistent synthetic thread is written. |
| Nested replies, reports, and spoiler labels | `Community.nestedReply.test.tsx`, visibility tests, and markup regression. | Synthetic browser flow verifies nested reply, individual report, `Spoilers flagged`, and `Spoilers`. | Content remains community context only. |
| Administrator moderation | admin authorization and visibility-filter tests. | Synthetic admin browser flow hides a reported reply and refreshes the report panel. | Real moderation remains restricted to authorized administrators. |
| Community ratings and reviews | `communityRating.test.ts`, title review report action test, and visibility filtering tests. | Synthetic browser verifies rating controls, report action, hidden-review state, and mobile containment. | No fabricated review or rating is seeded in production. |
| Responsive application shell and evidence labels | `accessibilityMarkup.test.ts` and keyboard suite. | Full-page desktop/mobile captures cover Discovery, Community, Recommendations, and TitlePage. | Current no-token routes retain candid source labels. |

| Workflow | Contract, unit, and accessibility evidence | Browser and responsive evidence | Production or managed-runtime evidence |
|---|---|---|---|
| Legal catalog and provider-first discovery | `catalog.test.ts`, `CatalogOfferPreview.test.tsx`, and `accessibilityMarkup.test.ts` preserve no-token and provider-hierarchy boundaries. | Public keyboard test verifies the labelled Discovery form and no-catalog result state. Desktop and mobile full-page captures verify Discovery layout and AI evidence compartment. | Standalone production HTTP checks have returned `200` for `/` and compiled assets. No-token mode remains candid. |
| Grounded AI public-web context | `aiDiscovery.test.ts` validates title correction, citation recovery, useful fallback, and legal-catalog separation. | Synthetic signed-in browser flow verifies correction, source rendering, loading, error, and mobile width containment. | AI requests remain server-side; the test does not claim a live research result. |
| Populated responsive controls | The relevant feature contracts are covered by `aiDiscovery.test.ts`, `recommendations.test.ts`, and `TitlePage.sourceBoundary.test.tsx`. | The synthetic browser suite changes to `375×812` after rendering a grounded source link, populated private post-watch card, and authenticated title rating/review controls; each stays visible and `document.documentElement.scrollWidth <= window.innerWidth`. | These are layout assertions against synthetic responses, not live catalog or member data. |
| Watchlist, snapshots, and provider alert selection | `availabilityTracking.test.ts` and `dataAccess.test.ts` validate confirmed-snapshot filtering, region scoping, and private member procedures. | Synthetic member flow verifies explicit watched recording and private-status retry. | Standalone production `/updates` route returned `200`; provider-account access is not implemented or claimed. |
| Private post-watch recommendations | `recommendations.test.ts`, `catalog.test.ts`, and `Watchlist.viewingSignal.test.tsx` validate explicit-signal behavior. | Synthetic member flow verifies loading, error, retry, populated card visibility, and mobile width containment. | No-token implementation candidly reports that live catalog picks are unavailable. |
| Community contributions and Leaving Soon leads | `dataAccess.test.ts`, `communityVisibility.test.ts`, and static markup regressions cover input, redaction, moderation, and unverified boundaries. | Desktop/mobile Community captures verify layout; synthetic flows cover signed-in title context, nested replies, reporting, and moderation action refresh. | Community leads are deliberately excluded from legal catalog, alerts, tracking, and decisions. |
| Spoiler-aware discussion | `Community.nestedReply.test.tsx` and `accessibilityMarkup.test.ts` cover nested and label semantics. | Synthetic browser flow asserts visible thread `Spoilers flagged` and reply `Spoilers` notices. | N/A; this is member content, not a background service. |
| Title detail, ratings, and reviews | `TitlePage.sourceBoundary.test.tsx`, `communityRating.test.ts`, and visibility tests cover source boundary, rating aggregate, review report action, and hidden output filtering. | Public no-catalog title test and synthetic review report/hidden-state browser flow cover controls; mobile test asserts visible rating control and no horizontal overflow. | Standalone production `/title/movie/1` route returned `200` in no-token mode. |
| Recommendation cards and related offers | `catalog.test.ts` and provider-preview tests cover deterministic/no-token behavior. | Public route browser check, desktop/mobile Recommendations capture, and synthetic populated private card mobile check. | Standalone production `/recommendations` route returned `200`. |
| Managed refresh scheduler | `scheduledRefresh.test.ts` validates no-token, orphan, and wrong-identity handling. | Not applicable. | Managed Heartbeat `g4LWHU9zMwiNtvRLKVJN2x` completed run `gEWXycVdsvzc4vA79qeCy6` with HTTP `200` and `catalogConfigured:false`; see `scheduler_verification.md`. |

## Current release baseline

The final release suite passes **17 Vitest files / 83 tests**, **8 Playwright checks**, TypeScript, and a production build. The built client still emits a non-fatal bundle-size warning. The catalog credential remains intentionally absent, so live legal offers and catalog-backed populated metadata are not claimed by any validation entry.
